export const checkPeople = (people: number)=>{
  if(people < 1){
    return "Can't be less than 1"
  }
}

export const checkBill = (bill: number)=>{
  if(bill < 1){
    return "Can't be less than 1"
  }
}